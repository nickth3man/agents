[project]
name = "chat"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "gradio>=5.29.1",
    "openai>=1.78.1",
    "pocketflow>=0.0.2",
    "python-dotenv>=1.0.0",
]

[tool.pytest.ini_options]
filterwarnings = [
    "ignore::DeprecationWarning:lark",
]
